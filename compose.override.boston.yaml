# Pré-cutover (migração Boston 2026-06-02): WAL archiving DESLIGADO até a Fase 6,
# para não conflitar com o host antigo que ainda arquiva na mesma stanza R2.
#
# TRANSITÓRIO: remover (ou deixar de passar com -f) assim que o pgBackRest for
# religado na VPS dedicada (stanza-create do cluster novo + backup full). A partir
# daí o compose.prod.yaml volta a valer sozinho (archive_mode=on).
#
#   docker compose --env-file deploy.env -f compose.prod.yaml -f compose.override.boston.yaml up -d
services:
  postgres:
    command: ["postgres"]
