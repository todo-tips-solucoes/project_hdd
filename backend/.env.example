# Template de configuração (sem segredos). Copie para .env e ajuste.
# Em produção, secrets vêm de /run/secrets (Docker Swarm), não deste arquivo.
HDD_PG_DSN=postgresql://hdd:hdd_dev@localhost:5433/hdd
HDD_LLM_DRIVER=subscription
# HDD_MODEL=haiku
HDD_LOG_LEVEL=INFO
