// hdd-worker entrypoint stub.
// Bootstrap real (env validation, DB init, server, FSM) entra em Story 1.a.7.
// Por enquanto apenas confirma que o build/runtime compila e executa.

console.log("hdd-worker placeholder — Story 1.a.7 wires bootstrap");
