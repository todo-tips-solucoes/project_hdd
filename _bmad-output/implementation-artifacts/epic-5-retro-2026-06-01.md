# Retrospectiva — Epic 5: Produção 24/7 & Conformidade

- **Data:** 2026-06-01
- **Projeto:** projeto_hdd (HORSE DRIVEN DEVELOPMENT — HDD v2)
- **Facilitação:** Amelia (Developer) · **Project Lead:** paulotodo
- **Primeira retrospectiva do projeto** · Epic 5 é a última do roadmap (não há Epic 6)
- **Nota de método:** o projeto implementou direto no código; não há `sprint-status.yaml`
  nem story-files com dev-notes. Esta retro foi ancorada nos artefatos reais: `epics.md`,
  os 7 commits do Epic 5, o tooling e as validações da sessão.

## Resumo de entrega

| Métrica | Valor |
|---|---|
| Histórias | 6/6 (100%) |
| Ordem executada | 5.3 → 5.4 → 5.1 → 5.2 → 5.6 → 5.5 |
| Commits | 7 (`d6f4dcb`..`2de3a61`), ~1786 inserções |
| Testes | 72 unit + 31 integração (novos: redaction, worker-loop, fila/quota, LGPD) |
| Tooling | ruff · mypy --strict · import-linter · openapi sem drift — verdes |
| Migrations | 0007 → 0009 |
| Validação real | Swarm dry-run 1 nó (api/worker/frontend/postgres 1/1, `/readyz` ok) · worker `healthy` · PITR local verde · CI verde no GitHub (6 jobs) |
| Incidentes em produção | 0 (sem produção ainda) |

## O que foi bem

- **Verificar em vez de assumir.** Descoberta empírica de que `env_prefix=HDD_` se aplica
  ao nome dos secrets (`hdd_pg_dsn`, não `pg_dsn`) — corrigiu uma premissa do prompt e
  evitou um bug silencioso de deploy.
- **Verificação adversarial real**, não mock: reaper de lease testado com TTL negativo
  (worker morto), crypto-shredding provado com decifragem que falha após o shred, PITR
  restaurando ao ponto-no-tempo correto.
- **Sequenciamento "barato → infra-pesado"**: 5.3 (CI) primeiro deu rede de segurança ao resto.
- **Defesa-em-profundidade**: redaction de logs como invariante independente de chamadas futuras.

## O que foi difícil

- **Imagem do worker**: combinar venv Python + Node + `gh` + `claude` num container exigiu
  copiar node da imagem oficial e baixar `gh` como binário → ficou **amd64-only**.
- **PITR falhou na 1ª tentativa**: backup full terminava frações de segundo antes do alvo;
  resolvido com margem (`sleep 2`). Timing em PITR é traiçoeiro.
- **Validação só local**: dry-run de Swarm e PITR com repo posix; **deploy real na Hetzner e
  D-032 (ToS da conta sob automação contínua) seguem por validar.**

## Descoberta significativa

"Todas as histórias prontas" **≠** "produto roda ponta a ponta". A infra está completa, mas
faltam **três fios de integração** entre os épicos E2↔E4↔E5 para uma feature percorrer
enqueue → claude → PR → gate → merge. Recomendação: tratar isto como um marco explícito
"Caminho até Produção" (ou um Epic 6 de integração) antes de declarar o M0/MVP pronto.

## Action items

### Caminho crítico — integração E2↔E4↔E5 (produto rodar ponta a ponta)
1. **Produtor da fila** — CLI `start` / API enfileira em `app.work_queue` (`WorkQueue.enqueue`)
   com payload `{task, thread_id}`. Owner: Dev. Hoje o worker consome, mas a fila fica vazia.
2. **Resume pós-gate** — aprovação no painel (`GateStore.resolve_authenticated`) deve chamar
   `orchestrator.resume(thread_id, decision)` do LangGraph. Owner: Dev.
3. **Verificador real** — substituir `runner._verify = True` por execução da suíte no sandbox
   (Story 2.3). Owner: Dev.

### Caminho até produção
4. **Deploy real na Hetzner** + smoke E2E (valida o AC 5.1 em produção e **D-032**). Owner: DevOps.
5. **Provisionar Cloudflare R2** e ligar o WAL archiving (fecha 5.5 em produção). Owner: DevOps.
6. **Base legal de transferência internacional** (Hetzner UE + Anthropic US) anexada ao ROPA
   — não-código (5.6). Owner: Controlador.

### Dívida técnica
7. **Imagem do worker amd64-only** — tornar multi-arch se houver alvo arm. Prioridade: baixa.
8. **API roda migrations no `command`** — corrida se `api` escalar além de 1 réplica; extrair
   job/etapa de migração dedicada antes de escalar. Prioridade: média.

### Processo / aprendizados a manter
- Manter verificação adversarial real onde agrega valor (provou pegar bugs).
- Verificar premissas de specs/prompts contra o sistema antes de codar.
- Se quiser rastreabilidade formal de dev-notes, avaliar adotar `sprint-status.yaml` +
  story-files do BMAD (hoje o histórico vive nos commits + memória do projeto).

## Avaliação de readiness — Epic 5

| Dimensão | Estado |
|---|---|
| Testes & Qualidade | ✅ Forte (unit + integração + CI verde). ⚠️ Falta E2E real. |
| Deployment | ⚠️ **Não deployado em produção** — só dry-run local. |
| Aceite do stakeholder | ✅ Operador revisou os diffs, aprovou ajustes, push feito. |
| Saúde técnica | ✅ Boa. ⚠️ 3 gaps de integração conhecidos. |
| Blockers | Nenhum no código. Produção depende dos itens 1–5 acima. |

## Próximos passos
1. Decidir o formato do "Caminho até Produção" (Epic 6 de integração vs. marco/stories avulsas).
2. Fechar os 3 fios de integração (itens 1–3).
3. Deploy real + smoke E2E na Hetzner (itens 4) — valida D-032.
4. Provisionar R2 (item 5).
