# Retrospetiva — Epic 2: Worker Autónomo & Pipeline Bimodal

**Data:** 2026-05-31 · **Âmbito:** fecho do Epic 2 (7/7 stories done) + preparação do Epic 3.
**Participantes (personas):** Amelia (Dev), John (PM), Mary (Analyst), Arquiteto, Dana (QA) · **Project Lead:** operador.
**Continuidade:** retro anterior `epic-1c-retro-2026-05-30.md` (action items AI-S0-1..4).

## 1. Métricas (verificáveis)

| Métrica | Valor |
|---|---|
| Stories | 7/7 done (2.1–2.7) |
| Tests | 304 → **351** (+47), 0 fail em todos os pushes |
| Integração | 16 pass / 3 skip (gated) |
| Commits | 14 (7× summary + 7× feat); CI verde em cada push |
| Deps novas | **0** em todo o épico |
| Dogfood | 13 → **20** summaries (503–557 words, todos ≤715) |
| Incidentes / Blockers | 0 / 0 |

## 2. Follow-through dos action items do Sprint 0 (AI-S0-*)

| # | Item | Status | Evidência |
|---|---|---|---|
| **AI-S0-1** | Sinal proativo de CI (`gh run watch` pós-push) | ✅ cumprido/internalizado | `gh run watch --exit-status` nas 7 stories; 0 CI vermelho despercebido |
| **AI-S0-2** | `runbook-completeness` no CI | ✅ cumprido | step "Runbook completeness (8×5)" passa em todos os runs |
| **AI-S0-3** | Registo único c/ TTL (`readiness-open-items.md`) | ✅ vivo/usado | +3 divergências registadas (2.4/2.6/2.7) nesta sessão |
| **AI-S0-4** | Reconciliar `epics.md` vs realidade | ⏳ **parcial** | tratado via registo per-story, **não** via reescrita do canon → escala para AI-E2-1 |

## 3. O que correu bem

- **4 chokepoints de AI Safety entregues como desenhados** (Pre-Mortem #2): apply-diff (2.3, Dev nunca escreve directo), gate diagnostics (2.4/2.5), confirmation-gate em acções irreversíveis (2.6). Composition-enforcement — `[[project-hdd-composition-risks]]` — é agora propriedade do sistema, não feature.
- **Disciplina mantida:** ~28 Open Questions com alternativas honestas; operador escolheu recomendadas ou delegou explicitamente (rec. fundamentada, nunca empurrada).
- **D-053 honrado:** fs real (mkdtemp), DB real (`:memory:`+migrations), apply-diff/ConfirmationGate reais nos testes; fake só o caro/não-determinístico (`claude -p`, spawn).
- **+47 testes, 0 deps, 0 fail, CI sempre verde.** Property tests (fast-check) na 2.3 (isolamento) e validador 2.4; FSM property-totalidade absorveu 2 novos estados/eventos (`gate_blocked`, `OperatorPaused`) sem regressão.
- **Pipeline bimodal completo:** gate Story→Dev (valida spec) + Dev→Review (valida output + retry→`RetryExhausted`) + lifecycle persistente + schemas Zod type-safe.

## 4. Tensões / o que rangeu

1. **Divergência `files_modified` recorreu 3×:** 2.4 (`fsm.ts`, na spec), 2.6 (`fsm.ts`, **fora** da spec), 2.7 (**conflito directo** spec×arquitectura no `verdict`). O conflito da 2.7 só foi apanhado na **última story** do épico. O registo evita rot silencioso, mas **adia a reconciliação** — o `epics.md` está mais divergente a cada épico (`[[feedback-hdd-soft-convention-rot]]` a manifestar-se).
2. **3 deferrals de extracção empilhados:** O-2.5-1 (`DiagnosticWriter`→port), O-2.6-1 (`RunStateRepository`), O-2.7-1 (reconciliar `devOutputSchema` da 2.3). Todos *"extrair quando aparecer o 2º/3º caller"*. O `DiagnosticWriter` **já vai em 2 callers** (gate→gate coupling). YAGNI correcto, mas o padrão de débito é real.
3. **Conflito verdict (2.7):** arquitectura e epics contradiziam-se num enum central; detectado tarde.

## 5. Insight central

O **registo de divergências** (AI-S0-3) é o mecanismo anti-rot que aguenta o peso — funciona, mas adia a reconciliação **indefinidamente**. Em algum momento ou se paga a dívida no canon, ou o `epics.md` deixa de ser fonte confiável. O Epic 2 atingiu esse ponto.

## 6. Action items (Epic 2 → Epic 3) — priorizados pelo Project Lead

| # | Action item | Decisão | Owner | Critério de sucesso |
|---|---|---|---|---|
| **AI-E2-1** | Reconciliar `epics.md` com a realidade registada (paga o AI-S0-4) | **correct-course pass ANTES do Epic 3** | PM/Dev + operador | `epics.md` alinhado; divergências 2.1/2.4/2.6/2.7 resolvidas no canon |
| **AI-E2-2** | Extrair abstracções partilhadas (DiagnosticWriter port, RunStateRepository, DevOutput) | **trigger explícito: ao 3º caller** (regra dura registada) | Dev | extracção acontece no 3º consumidor, não antes nem por acumulação indefinida |
| **AI-E2-3** | Spot-check arquitectura×epics×memórias para o schema clihelper (O-B5-3) | **Sim — no create-story da 3.1** | Dev/Analyst | conflito apanhado no arranque da 3.1, não no fim do Epic 3 |

> **Nota de processo:** o conflito verdict (2.7) prova que o spot-check (AI-E2-3) e a reconciliação do canon (AI-E2-1) atacam a mesma raiz — inconsistência planning-artifact não detectada. AI-E2-1 deve correr **antes** da 1ª story do Epic 3.

## 7. Preparação do Epic 3 — Canal WhatsApp (clihelper) + Fallback E-mail (6 stories)

**Readiness (verificada):**
- Story 3.1 (`OutboundNotifyPort` + clihelper adapter) `blocked_by: [1.a.3, 1.a.5]` → **done** ✅
- Acoplamento a Epic 2: **mínimo** — Epic 3 é maioritariamente independente (outbound messaging; HTTP client sobre clihelper). Nenhuma descoberta do Epic 2 quebra o plano do Epic 3.

**Atenção (não-bloqueante):**
- **O-B5-3** (schema webhook-mock clihelper) torna-se relevante (inbound, Epic 3) — alvo do spot-check AI-E2-3.
- Memórias a cruzar: `[[project-hdd-clihelper-integration]]` (HDD = HTTP client, rate-limit 1 req/s), `[[project-hdd-n8n-topology]]` (clihelper APENAS outbound; n8n inbound aggregator; drop-at-ingress Zod), `[[project-hdd-whatsapp-api]]` (Meta Cloud API via clihelper).
- **O-C2-1** (wire `CLIHELPER_TOKEN`) entra em jogo no Epic 3.

**Significant discovery alert:** ❌ nenhuma — o plano do Epic 3 mantém-se sólido. (Mas AI-E2-1 deve correr antes para o canon estar fiável.)

## 8. Readiness assessment do Epic 2

| Dimensão | Estado |
|---|---|
| Testes & qualidade | ✅ 351 pass / 0 fail; integração 16 pass; type-check + lint limpos |
| CI / deploy | ✅ verde em todos os 7 pushes; binário compila (`build:compile`) |
| Aceitação | ✅ revisão humana obrigatória em todas as 7 (0 auto-aprovações) |
| Saúde técnica | ✅ estável; 3 deferrals de extracção logados (não-bloqueantes) |
| Blockers | nenhum bloqueante; 3 action items + open items com plano |

## 9. Takeaways

1. **O registo anti-rot funciona, mas não substitui reconciliação.** Atingimos o ponto em que o canon precisa de uma passagem — AI-E2-1.
2. **Conflitos planning-artifact (spec×arquitectura) são silenciosos** até alguém os tocar; o spot-check antecipado (AI-E2-3) é a defesa.
3. **Deferrals de extracção precisam de trigger explícito** (regra-do-3º-caller, AI-E2-2) — senão acumulam como débito invisível.
4. **AI Safety por composição é agora muscle memory** — 4 chokepoints num épico, todos wired e testados.

**Próximos passos:** (1) **AI-E2-1 — correct-course do `epics.md`** antes de tudo; (2) arrancar Epic 3 pela Story 3.1 com o spot-check AI-E2-3; (3) rever AI-E2-2 (trigger-3º-caller) quando o 3º consumidor de qualquer abstracção aparecer.
